Bundles are ZIP files of configuration and data files. They are not installed as plugins. Instead, when a node starts, {{ecloud}} makes the bundle contents available in the node's configuration directory.

Use a bundle when every node needs the same files, such as:

* Synonym, stop-word, or compound-word dictionaries
* Scripts referenced in queries
* Cluster configuration files, such as SAML metadata
