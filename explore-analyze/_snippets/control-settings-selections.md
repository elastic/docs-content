Selections
:   Choose how many values a viewer can select at once:
    - **Allow multiple selections** (default): viewers can filter on several values at the same time.
    - **Only allow a single selection**: viewers can filter on one value at a time. A new selection replaces the previous one.
