---
navigation_title: How vector search works
applies_to:
  stack:
  serverless:
products:
  - id: elasticsearch
  - id: cloud-serverless
  - id: cloud-hosted
  - id: cloud-enterprise
  - id: cloud-kubernetes
  - id: elastic-stack
---
# How vector search works

Vector search finds results based on _meaning_ rather than exact keyword matches. This page explains the core concepts and terminology you need before working with vector search in {{es}}.

## Vectors and embeddings

### Vector

A vector is an ordered list of numbers that represents data in a multi-dimensional space. Each number in the list is a coordinate along one dimension. In the context of search, vectors encode the semantic meaning of content such as text, images, or audio. Content with similar meaning is mapped to nearby points in this space, so proximity between vectors indicates similarity.

### Embedding

An embedding is a vector that has been generated by a machine learning model to capture semantic information. The model is trained so that content with similar meaning produces embeddings that are close together in vector space. For example, the phrases "budget hotels" and "affordable places to stay" would have embeddings near each other even though they share no words.

### Embedding model

An embedding model is a machine learning model that converts your source data into vector embeddings. The model you choose determines the dimensionality and quality of the resulting vectors. It also constrains what types of content the system understands well. The vectors in your index and your query vectors must be generated by the same model for similarity comparisons to be meaningful.

### Dimensions

Dimensions are the number of elements in a vector. Each dimension corresponds to one coordinate in the vector space. Dense embedding models typically produce vectors with hundreds or thousands of dimensions (for example, 384, 768, or 1536). Higher dimensions can capture more nuance but use more memory and compute. The dimension count is fixed by the model and must match between stored vectors and query vectors.

## Dense vectors vs. sparse vectors

{{es}} supports two types of vector representations, each suited to different use cases and implementation patterns.

### Dense vectors

Dense vectors are fixed-length arrays where every element has a value. They are produced by neural embedding models that learn to map content into a continuous space. Dense vectors capture overall semantic meaning and work well for natural language understanding, multilingual content, and rich semantic matching. They typically have hundreds or thousands of dimensions.

In {{es}}, dense vectors use the [`dense_vector`](dense-vector.md) field type and are queried with the [`knn`](knn.md) query. You can deploy external or hosted embedding models, or [bring your own pre-computed vectors](bring-own-vectors.md).

### Sparse vectors

Sparse vectors are arrays where most elements are zero. Only a small number of dimensions carry meaningful values, each corresponding to a specific term or concept. Sparse vectors are often used for lexical-style matching with semantic expansion: content is expanded into weighted terms that capture related concepts. Results tend to be more explainable because you can see which terms contributed to the match.

In {{es}}, sparse vectors are generated by the [ELSER](/explore-analyze/machine-learning/nlp/ml-nlp-elser.md) (Elastic Learned Sparse Encoder) model and use the [`sparse_vector`](sparse-vector.md) field type. ELSER is built-in and requires no external model deployment.

## Quantization and compression

Embedding models usually output floating-point vectors (for example, 32 bits per dimension). At scale, these vectors consume substantial memory and can slow search. Quantization is a form of lossy compression that reduces the precision of vector values. It trades a small amount of accuracy for lower memory use and faster similarity computations. For production workloads with millions or billions of vectors, quantization is often essential to keep latency and cost manageable.

### Quantization strategies in {{es}}

{{es}} offers several quantization options for `dense_vector` fields: [BBQ (Better Binary Quantization)](elasticsearch://reference/elasticsearch/mapping-reference/dense-vector.md#dense-vector-quantization-bbq), [int8](elasticsearch://reference/elasticsearch/mapping-reference/dense-vector.md#dense-vector-quantization-int8), and [int4](elasticsearch://reference/elasticsearch/mapping-reference/dense-vector.md#dense-vector-quantization-int4). For the full list, configuration details, and trade-offs, refer to [Automatically quantize vectors for kNN search](elasticsearch://reference/elasticsearch/mapping-reference/dense-vector.md#dense-vector-quantization).

## Next steps

- [Semantic search](../semantic-search.md): managed workflows using `semantic_text` and the {{infer-cap}} API
- [Dense vector search](dense-vector.md): manual dense vector implementation
- [Sparse vector search](sparse-vector.md): ELSER-based semantic search
- [kNN search](knn.md): approximate and exact k-nearest neighbor search
